{% extends 'base_admin.html.twig' %}

{% block title %}Add Staff | Sweetoria{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/admin_staff.css') }}">
{% endblock %}

{% block body %}
<div class="staff-workspace staff-form-page">
    <header class="staff-hero">
        <div class="staff-hero-content">
            <p class="staff-kicker">Team Operations</p>
            <h1 class="staff-title">Add Staff Account</h1>
            <p class="staff-subtitle">Create a new team login with role and status settings for the Sweetoria admin workspace.</p>
        </div>
        <a href="{{ path('admin_staff_index') }}" class="staff-btn-secondary">
            <i class="bi bi-arrow-left"></i> Back to Staff
        </a>
    </header>

    <div class="staff-form-card">
        {{ form_start(form, {
            action: path('admin_staff_new'),
            method: 'POST',
            attr: { class: 'staff-form' }
        }) }}

        {{ form_errors(form) }}

        <div class="staff-form-grid">
            <div class="staff-field">
                {{ form_label(form.username) }}
                {{ form_widget(form.username, { attr: { class: 'form-control', placeholder: 'username@example.com' } }) }}
                {{ form_errors(form.username) }}
            </div>

            <div class="staff-field">
                <label class="form-label">Password</label>
                {{ form_widget(form.plainPassword, { attr: { class: 'form-control', placeholder: 'Secure password' } }) }}
                {{ form_errors(form.plainPassword) }}
            </div>

            <div class="staff-field" style="grid-column: 1 / -1;">
                {{ form_label(form.roles) }}
                <div class="staff-role-pills">
                    {% for child in form.roles %}
                        <label class="staff-role-pill">
                            {{ form_widget(child) }}
                            <span>{{ child.vars.label }}</span>
                        </label>
                    {% endfor %}
                </div>
                {{ form_errors(form.roles) }}
            </div>

            <div class="staff-field">
                {{ form_label(form.status) }}
                {{ form_widget(form.status, { attr: { class: 'form-select' } }) }}
                {{ form_errors(form.status) }}
            </div>
        </div>

        {{ form_widget(form._token) }}

        <div class="staff-form-actions">
            <button type="submit" class="staff-btn-primary">
                <i class="bi bi-person-plus-fill"></i> Create Staff Account
            </button>
            <a href="{{ path('admin_staff_index') }}" class="staff-btn-secondary">Cancel</a>
        </div>

        {{ form_end(form, { render_rest: false }) }}
    </div>
</div>
{% endblock %}
